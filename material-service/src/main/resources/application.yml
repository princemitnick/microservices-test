server:
  port: 8084

spring:
  application:
    name: MATERIAL-SERVICE

eureka:
  client:
    register-with-eureka:
      fetch-registry: true
      service-url:
        defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost